import { Card, Input } from "@appquality/appquality-design-system";

export const FormSearchCard = () => {
  return (
    <Card title="Ricerca form">
      <Input
        placeholder="Search"
        type="search"
        id="search"
        onChange={() => {}}
      />
    </Card>
  );
};
